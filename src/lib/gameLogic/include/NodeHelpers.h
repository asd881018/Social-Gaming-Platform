
#pragma once

// #include "DataBucket.h" 

class NodeHelpers {
public:
    // static void sendToAllPlayers(classifier::MessageClassifier& serverHandle, const DummyBucket& data, const std::string& message);

    // // Send a request to the server for user input
    // static void sendUserInputRequest(classifier::MessageClassifier& serverHandle, DummyBucket& data, const int dummyIndex);
};